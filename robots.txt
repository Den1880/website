User-agent: *
Allow: /

# /go/* are booking handoffs to Optix (302 out to den1880.optixapp.com), not content.
# Blocked so they can't bloat the index. The links are also rel="nofollow" and the
# redirect sends X-Robots-Tag: noindex. Belt and braces.
Disallow: /go/

# The preferred vendor sheet is password protected at the edge (see
# netlify/edge-functions/vendor-gate.js) and sends X-Robots-Tag: noindex. Blocked
# here too so the sign-in page itself never lands in the index.
Disallow: /preferred-vendors

Sitemap: https://den1880.co/sitemap.xml
